<%= turbo_frame_tag :submission do %>
  <div class="flex gap-2 align-center">
    <div class="text-xs flex items-center">You will not be able to make any changes,<br>once answers are submiited. Are you sure?</div>
    <%= link_to "Yes", exam_submissions_path(exam_id: @exam.id), class: "bg-green-500 hover:bg-green-700 text-white font-bold py-2 px-8 rounded", target: "_top" %>
    <%= button_to "No", cancel_exam_submissions_path(exam_id: @exam.id), method: :get, class: "bg-red-500 hover:bg-red-700 text-white font-bold py-2 px-8 rounded" %>
  </div>
<% end %>
